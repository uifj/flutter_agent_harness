name: agent_harness
description: "A Flutter desktop agent harness: chat UI, tool-call projection via Genkit, and a split-pane workbench."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lucide: ^1.33.0
  flutter_pty: ^0.4.2
  genkit: ^0.15.1
  genkit_anthropic: ^0.2.11
  genkit_google_genai: ^0.2.12
  genkit_mcp: ^0.2.4
  genkit_middleware: ^0.5.1
  genkit_openai: ^0.3.7
  gpt_markdown: ^1.2.1
  path: ^1.9.1
  path_provider: ^2.1.6
  re_editor: ^0.10.0
  re_highlight: ^0.0.3
  schemantic: ^0.2.2
  xterm: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The framework's own widgets reach for MaterialIcons (PopupMenuButton's
  # default chevron, selection handles); app code uses Lucide via
  # flutter_lucide.
  uses-material-design: true

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
